```{r}
# Read first
  file_path <- paste0("house_person_data/house_person_", 2019, ".csv")
  house_person <- read.csv(file_path)
  
# Range of next years
  years <- 2020:2024
  
for (year in years) {
  
  file_path <- paste0("house_person_data/house_person_", year, ".csv")
  second_file <- read.csv(file_path)
  
  house_person <- rbind(house_person, second_file)
}
```
